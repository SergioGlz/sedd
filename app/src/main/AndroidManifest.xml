<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" >

    <application
        android:name=".framework.android.app.SeddApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SEDD"
        tools:targetApi="31" >
        <activity
            android:name=".framework.android.ui.splash.SplashScreenActivity"
            android:exported="true"
            android:theme="@style/Theme.Starting">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".framework.android.ui.login.LoginActivity"
            android:exported="false"
            android:label="@string/splashscreen_title"/>
        <activity
            android:name=".framework.android.ui.student.home.StudentHomeActivity"
            android:exported="false"
            android:theme="@style/Theme.SEDD.NoActionBar"
            android:label="@string/splashscreen_title"/>
    </application>

</manifest>